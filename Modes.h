#ifndef MODES_H
#define MODES_H

#include "WordArray.h"

void fileMode(WordArray &wordArray);
void stdinMode(WordArray &wordArray);
void resizeMode(WordArray &wordArray);
void printMode(WordArray &wordArray);

#endif